/*

This could do the introspection api call for the entrypoint (only)
and pass the result to options for calculang-transform-loader

I don't need HMR in the model compilation (no big state concerns), moreso the app itself.

*/
